# Supabase — the only backend (auth + data)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# Stripe (test mode keys — get these from https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Demo login is ON by default (no env needed). Demo emails get a client-side
# mock session + fixture dashboard data. Set to "false" when connecting real
# Supabase auth. See src/lib/auth/roles.ts and src/lib/auth/demoData.ts.
# NEXT_PUBLIC_DEMO_AUTH="false"

NEXT_PUBLIC_APP_NAME="FUJRS"
